# MOLTBOT Portal — Production Environment
# Set VITE_API_URL to your Cloudflare Tunnel URL once configured
VITE_API_URL=http://localhost:5080
# Token is set via localStorage in the browser (not here for security)
